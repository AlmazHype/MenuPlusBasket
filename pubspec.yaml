name: menu_plus_basket
description: A Menu Plus Basket test project.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  get_it: ^7.2.0
  
  equatable: ^2.0.3

  dartz: ^0.10.1

  flutter_bloc: ^8.0.1

  freezed_annotation: ^2.1.0

  json_serializable: ^6.2.0
  json_annotation: ^4.6.0

  shimmer: ^2.0.0
  flutter_spinkit: ^5.1.0
  cached_network_image: ^3.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  freezed: ^2.1.0+1
  build_runner: ^2.2.0

  flutter_lints: ^2.0.1


flutter:
  uses-material-design: true
  generate: true


  assets:
    - assets/libraries/products.json

